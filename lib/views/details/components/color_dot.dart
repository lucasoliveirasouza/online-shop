import 'package:flutter/material.dart';

class ColorDot extends StatelessWidget {
  const ColorDot({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
